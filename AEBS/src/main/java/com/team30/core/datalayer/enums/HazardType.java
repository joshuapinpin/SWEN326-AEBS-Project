package com.team30.core.datalayer.enums;

/**
 * Enum representing different types of hazards that the AEBS system can detect and respond to.
 * Defines categories such as OBJECT_ENTERS_ROAD, SENSOR_FAILURE, WEATHER_CHANGE, and LIGHT_CHANGE for potential hazards in the driving environment.
 */
public enum HazardType {
    OBJECT_ENTERS_ROAD,
    SENSOR_FAILURE,
    WEATHER_CHANGE,
    LIGHT_CHANGE,
    BRAKE_FAILURE
}
