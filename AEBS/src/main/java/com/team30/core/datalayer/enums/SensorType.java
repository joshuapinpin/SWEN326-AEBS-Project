package com.team30.core.datalayer.enums;

/**
 * Enum representing different types of sensors used in the AEBS system.
 * Defines the categories of sensors that can provide data to the system, such as RADAR, LIDAR, CAMERA, and WHEEL_SPEED.
 */
public enum SensorType {
    RADAR,
    LIDAR,
    CAMERA,
    WHEEL_SPEED
}