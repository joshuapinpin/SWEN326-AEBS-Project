package com.team30.core.datalayer.enums;

/**
 * Enum representing the driving modes in the AEBS system.
 * Defines the current state of the vehicle's driving behavior, categorized as CRUISING, BRAKING, and RESUMING.
 */
public enum DrivingMode {
    CRUISING,
    BRAKING,
    RESUMING,
    FAIL_SAFE
}
