package com.team30.core.datalayer.enums;

/**
 * Enum representing different types of objects that the AEBS system can detect and classify.
 * Defines categories such as VEHICLE, PEDESTRIAN, and UNKNOWN for objects in the environment.
 */
public enum ObjectType {
    VEHICLE,
    PEDESTRIAN,
    UNKNOWN
}
